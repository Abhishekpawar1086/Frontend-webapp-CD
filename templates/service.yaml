apiVersion: v1
kind: Service
metadata:
  name: frontend-webapp-service
  namespace: frontend-app
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: frontend-webapp
  type: LoadBalancer
